[graph]
all-features = true
no-default-features = false

[output]
feature-depth = 3

[advisories]
db-path = "~/.cargo/advisory-db"
db-urls = [ "https://github.com/rustsec/advisory-db" ]
ignore = [ ]
maximum-db-staleness = "P1D"
git-fetch-with-cli = true
unmaintained = "all"
unsound = "all"
unused-ignored-advisory = "deny"
yanked = "deny"

[licenses]
# Only licenses actually present in the dependency graph may be listed here:
# unused-allowed-license = "deny" rejects entries nothing uses. The crate itself
# is Apache-2.0; add MIT / Unicode-3.0 / etc. as dependencies that carry them
# are introduced.
allow = [ "Apache-2.0", "MIT", "Unicode-3.0" ]
unused-allowed-license = "deny"
confidence-threshold = 0.95

[bans]
multiple-versions = "deny"
wildcards = "deny"
highlight = "all"

[[bans.deny]]
crate = "openssl"
wrappers = [ ]

[[bans.deny]]
crate = "openssl-sys"
wrappers = [ ]

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = [ "https://github.com/rust-lang/crates.io-index" ]
allow-git = [ ]

[sources.allow-org]
github = [ ]
gitlab = [ ]
bitbucket = [ ]
